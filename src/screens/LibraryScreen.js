// src/screens/LibraryScreen.js — placeholder, será desenvolvido a seguir
import { colors } from "../AppScreens";

function LibraryScreen() {
  return (
    <div style={{ padding: "20px", color: colors.text }}>
      <p>📚 Biblioteca — em construção</p>
    </div>
  );
}
export default LibraryScreen;