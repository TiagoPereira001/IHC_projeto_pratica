// src/screens/HomeScreen.js — placeholder, será desenvolvido a seguir
import { colors } from "../AppScreens";

function HomeScreen() {
  return (
    <div style={{ padding: "20px", color: colors.text }}>
      <p>🎹 Gerador — em construção</p>
    </div>
  );
}
export default HomeScreen;